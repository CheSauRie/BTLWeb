const express = require('express');


const stockRouter = express.Router();


// stockRouter.get("/", getAllProductLine)
// stockRouter.post("/", createProductLine)
// stockRouter.get("/:id", getDetailProductLine)
// stockRouter.put("/:id", updateProductLine)
// stockRouter.delete("/:id", deleteProductLine)


module.exports = {
    stockRouter
}